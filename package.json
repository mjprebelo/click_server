{
  "name": "rho exercise",
  "version": "1.0.0",
  "description": "WebSocket exercise, to update number of clicks in every connected client",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mjprebelo",
  "license": "ISC",
  "dependencies": {
    "ws": "^1.1.1"
  }
}
